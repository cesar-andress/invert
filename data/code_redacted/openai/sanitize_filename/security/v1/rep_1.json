{
  "original_path": "/home/cesar/papers/invert/invert/data/code/openai/sanitize_filename/security/v1/rep_1.py",
  "redacted_path": "/home/cesar/papers/invert/invert/data/code_redacted/openai/sanitize_filename/security/v1/rep_1.py",
  "replacements": [
    {
      "stage": "docstrings",
      "action": "removed"
    },
    {
      "stage": "identifiers",
      "mapping": []
    }
  ],
  "removed_lines": [
    "def sanitize_filename(name: str) -> str:",
    "    if not isinstance(name, str) or not name:",
    "        raise ValueError('Input must be a non-empty string.')",
    "    safe_name = ''.join((c if c.isalnum() or c in ('_', '-') else '_' for c in name)).strip()",
    "    return safe_name if safe_name else 'default_filename'"
  ],
  "redaction_version": "strict_v1",
  "identifier_mapping": []
}